/*
  Community edition stub (owner ruling 2026-08-28).

  The blog (public pages + the admin CMS that authors them) is a hosted
  surface — a self-hosted install has no nodespec.io blog to render, and its
  routes are already gated to the hosted edition. This stub satisfies
  App.tsx's static import; the components never render in this build.
*/
export function BlogPage(): null {
  return null;
}

export function BlogPostPage(): null {
  return null;
}

export function BlogContentRenderer(): null {
  return null;
}
