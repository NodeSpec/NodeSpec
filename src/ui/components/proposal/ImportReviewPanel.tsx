/*
  Community edition stub — the repo-import review surface belongs to the
  reverse-engineering pipeline, which is not part of the open-source
  distribution (available on NodeSpec hosted Indie+ and enterprise builds).
  Renders nothing; the editor mounts it unconditionally and the community
  build simply never produces an import proposal for it to show.
*/
export function ImportReviewPanel(_props: { [key: string]: unknown }) {
  return null;
}
